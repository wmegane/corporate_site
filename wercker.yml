# This references the default Ruby container from
# the Docker Hub.
# https://registry.hub.docker.com/_/ruby/
# If you want to use a specific version you would use a tag:
# ruby:2.2.2
# versionを指定しないと2.4.0が使用され、middleman buildコマンドでエラーが発生する。
box: ruby:2.2.2
# You can also use services such as databases. Read more on our dev center:
# http://devcenter.wercker.com/docs/services/index.html
# services:
    # - postgres
    # http://devcenter.wercker.com/docs/services/postgresql.html

    # - mongo
    # http://devcenter.wercker.com/docs/services/mongodb.html

# This is the build pipeline. Pipelines are the core of wercker
# Read more about pipelines on our dev center
# http://devcenter.wercker.com/docs/pipelines/index.html
build:
    # Steps make up the actions in your pipeline
    # Read more about steps on our dev center:
    # http://devcenter.wercker.com/docs/steps/index.html
    steps:
        - bundle-install
production-deploy:
  steps:
    - bundle-install:
        jobs: 4
    - script:
        name: set env
        code: export RUBYOPT=-EUTF-8
    - script:
        name: html code build
        code: middleman build
    - script:
        name: deploy use ftp
        code: middleman deploy
  # after-steps:
  #   - slack-notifier:
  #       url: https://hooks.slack.com/services/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
  #       channel: general
  #       username: wercker